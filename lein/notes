;;   :init
;;   (do
;;     (use '[[table.core :only (table)]
;;            ;(use 'desc.core)
;;            [rubydoc.core]
;;            [clojure.repl :only (dir-fn)]])

;;     (load-file
;;      (str (System/getProperty "user.home") "/.lein/user.clj")))}
         ;;      }
         ;;
;; user.clj
;         [cheshire "5.2.0"]
         ;[ritz/ritz-nrepl-middleware "0.7.0"]
         ;[table "0.4.0"]
         ;;[desc "0.1.0"]
         ;[rubydoc "0.4.0"]]
